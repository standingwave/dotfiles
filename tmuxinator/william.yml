# ~/.tmuxinator/dotcom.yml

name: william
root: /Users/apandit/dev/william


# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# pre: sudo /etc/rc.d/mysqld start

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
# pre_window: rbenv shell 2.0.0-p247

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers like byobu.
# tmux_command: byobu

# Specifies (by name or index) which window will be selected on project startup. If not set, the first window is used.
# startup_window: logs

windows:
  - code:
      layout: 9f82,255x64,0,0{173x64,0,0,4,81x64,174,0,5}
      panes:
        - vim
        - gcom && gu && bmr
  - william:
      layout: main-vertical
      panes:
        - william_logs:
          - ssh vm
          - ssh billing
          - tailf /etc/sv/william/log/main/current
        - william_worker_logs:
          - ssh vm
          - ssh billing
          - tailf /etc/sv/william-workers/log/main/current
  - nsq:
      layout: tiled
      panes:
        - nsqd_logs:
          - ssh vm
          - ssh billing
          - tailf /etc/sv/nsqd/log/main/current
        - nsqlookupd_logs:
          - ssh vm
          - ssh billing
          - tailf /etc/sv/nsqlookupd/log/main/current
        - nsq_stats:
          - ssh vm
          - ssh billing
          - sleep 5
          - echo "while true; do curl http://billing:1911/stats; sleep 1; done;"
        - nsq_clear:
          - ssh vm
          - ssh billing
          - curl -X POST http://billing:1911/topic/delete\?topic=minitest
